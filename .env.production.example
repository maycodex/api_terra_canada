# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ============================================
# IMPORTANTE: Copia este archivo como .env en producción
# y configura los valores reales

# ============================================
# SERVIDOR
# ============================================
NODE_ENV=production
PORT=3000
API_VERSION=v1

# ============================================
# BASE DE DATOS (PostgreSQL)
# ============================================
# IMPORTANTE: Cambia estos valores por tu base de datos real
DATABASE_URL=postgresql://usuario:password@host:puerto/terra_canada_v2

# Ejemplo con servidor remoto:
# DATABASE_URL=postgresql://terra_user:secure_password@epanel.salazargroup.cloud:5433/terra_canada_v2?sslmode=require

# ============================================
# JWT (CAMBIAR EN PRODUCCIÓN)
# ============================================
# CRÍTICO: Genera un secreto seguro de al menos 32 caracteres
# Puedes usar: openssl rand -base64 32
JWT_SECRET=CAMBIAR_ESTE_SECRETO_SUPER_SEGURO_EN_PRODUCCION_MIN_32_CARACTERES_ALEATORIOS
JWT_EXPIRES_IN=8h
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# N8N WEBHOOKS
# ============================================
N8N_BASE_URL=https://n8n.salazargroup.cloud
N8N_WEBHOOK_DOCUMENTO=/webhook/procesar-documento
N8N_WEBHOOK_CORREO=/webhook/enviar-gmail
N8N_AUTH_TOKEN=
# Token para webhooks entrantes de N8N
N8N_WEBHOOK_TOKEN=CAMBIAR_TOKEN_SECRETO_N8N_WEBHOOK_MIN_32_CARACTERES

# ============================================
# UPLOAD DE ARCHIVOS
# ============================================
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760
ALLOWED_MIME_TYPES=application/pdf

# ============================================
# STORAGE (Opcional - para cloud)
# ============================================
STORAGE_TYPE=local
# Si usas S3, descomentar y configurar:
# AWS_ACCESS_KEY_ID=tu_access_key
# AWS_SECRET_ACCESS_KEY=tu_secret_key
# AWS_S3_BUCKET=terra-canada-documents
# AWS_REGION=us-east-1

# ============================================
# CORREO (Backup - si N8N falla)
# ============================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@terracanada.com
SMTP_PASS=

# ============================================
# LOGS
# ============================================
LOG_LEVEL=info
LOG_DIR=/app/logs

# ============================================
# SEGURIDAD
# ============================================
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# CORS
# ============================================
# Cambia por el dominio de tu frontend
CORS_ORIGIN=https://app.terracanada.com

# ============================================
# NOTAS IMPORTANTES:
# ============================================
# 1. NUNCA subas este archivo con valores reales a Git
# 2. Genera secretos únicos para JWT_SECRET y N8N_WEBHOOK_TOKEN
# 3. Usa SSL/TLS para la conexión a la base de datos en producción
# 4. Configura CORS_ORIGIN con el dominio real de tu frontend
# 5. Considera usar un gestor de secretos (AWS Secrets Manager, Vault, etc.)
